handler: src/handlers/keto/cronDailyMotivation.handler
memorySize: 128
timeout: 60
events:
  - schedule:
      rate: cron(0 15 * * ? *)
      enabled: true
      name: ketoflow-crondailymotivation-${sls:stage}
      description: Animo diario 9am MX
