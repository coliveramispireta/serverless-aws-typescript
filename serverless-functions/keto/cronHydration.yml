handler: src/handlers/keto/cronHydration.handler
memorySize: 128
timeout: 60
events:
  - schedule:
      rate: cron(0 20 * * ? *)
      enabled: true
      name: ketoflow-cronhydration-${sls:stage}
      description: Hidratacion diaria 2pm MX
