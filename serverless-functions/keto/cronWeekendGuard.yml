handler: src/handlers/keto/cronWeekendGuard.handler
memorySize: 128
timeout: 60
events:
  - schedule:
      rate: cron(0 23 ? * FRI *)
      enabled: true
      name: ketoflow-cronweekendguard-${sls:stage}
      description: Guardia fin de semana viernes 5pm MX
