handler: src/handlers/keto/postAuthLinkFederated.handler
memorySize: 256
timeout: 10
events:
  - cognitoUserPool:
      pool: ${self:provider.environment.USER_POOL_NAME}
      trigger: PostAuthentication
      existing: true